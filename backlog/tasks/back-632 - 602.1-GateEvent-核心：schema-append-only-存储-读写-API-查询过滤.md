---
id: BACK-632
title: 602.1 - GateEvent 核心：schema + append-only 存储 + 读写 API + 查询过滤
status: 'Basic: Done'
assignee:
  - '@claude'
created_date: '2026-07-05 09:05'
updated_date: '2026-07-06 03:46'
labels: []
dependencies: []
ordinal: 50000
pipeline_id: execution
phase: done
parent_id: BACK-602
dod:
  - text: bunx tsc --noEmit
    checked: false
  - text: bun run check .
    checked: false
  - text: bun test src/test/gate-event-store.test.ts
    checked: false
role: primitive
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
新模块（如 src/core/gate-event-store.ts）：GateEvent 类型（{id, item_id, pipeline_id, gate, actor, verdict, timestamp, payload}）；appendGateEvent（append-only，从不改写/删除已写行）；queryGateEvents（按 pipeline_id/gate/actor/时间范围过滤 + 分页）。Acceptance 含 negative control：(1) append-only——构造一次试图改写/删除已写 event 的调用路径，确认被拒绝或 API 根本不提供；(2) core 不解释 payload——构造含 E/C/H/gcl 字段名的 payload，grep+走查确认代码里无对应字符串匹配/特判；(3) 真实并发写测试（非 mock 锁）确认写入不丢失/不交错；(4) 查询过滤单测覆盖 pipeline_id/gate/actor/时间范围/分页。bunx tsc --noEmit && bun run check . 绿。依赖：无。详见 BACK-602 plan 的 602.1 节。
<!-- SECTION:DESCRIPTION:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [ ] #1 bunx tsc --noEmit passes when TypeScript touched
- [ ] #2 bun run check . passes when formatting/linting touched
- [ ] #3 bun test (or scoped test) passes
<!-- DOD:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
claimed: 2026-07-05T09:09:49Z

BACK-634 backfill: dodGates were missing at decompose time (root cause of the first engine complete -> needs-human misfire); added retroactively so engine complete can now adjudicate for real.
<!-- SECTION:NOTES:END -->
